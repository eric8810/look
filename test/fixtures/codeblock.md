# Code Block Highlighting

This file contains a TypeScript code block.

```ts
interface User {
  id: number;
  name: string;
}

function greet(u: User): string {
  return `Hello, ${u.name}!`;
}
```

And a Python block too.

```python
def hello(name: str) -> str:
    return f"Hello, {name}!"
```

End of file.
